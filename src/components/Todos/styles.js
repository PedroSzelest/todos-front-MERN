import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  mainContainer: {
    margin: "200px",
    display: 'flex',
    alignItems: 'center',
  },
  actionDiv: {
    textAlign: 'center',
  },
}));
